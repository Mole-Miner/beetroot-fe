const current = 40;
const max = 45;

/*if (current > max) {
    console.log(`${current} > ${max}`);
}

if (current > max)
    console.log(`${current} > ${max}`);

if (current > max) console.log(`${current} > ${max}`);*/

/*if (current < max) {
    console.log(`${current} < ${max}`);
} else {
    console.log(`${current} > ${max}`);
}

if (current < max)
    console.log(`${current} < ${max}`);
else
    console.log(`${current} > ${max}`);

if (current < max) console.log(`${current} < ${max}`);
else console.log(`${current} > ${max}`);*/

/*if (current < max) {
    console.log(`${current} < ${max}`);
} else if (current === max) {
    console.log(`${current} === ${max}`);
} else {
    console.log(`${current} > ${max}`);
}

if (current < max)
    console.log(`${current} < ${max}`);
else if (current === max)
    console.log(`${current} === ${max}`);
else
    console.log(`${current} > ${max}`);

if (current < max) console.log(`${current} < ${max}`)
else if (current === max) console.log(`${current} === ${max}`);
else console.log(`${current} > ${max}`);*/

// const data = current > max ? `${current} > ${max}` : `${current} <= ${max}`;
// console.log(data);
