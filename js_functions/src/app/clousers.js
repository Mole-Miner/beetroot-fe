/*function calculator() {
    const first = 11;
    const second = 24;
    function multiply() {
        return first * second;
    }
    return multiply;
}

const calcMul = calculator();
console.log(calcMul());*/

/*const calculator = function () {
    const first = 11;
    const second = 24;
    const multiply = function () {
        return first * second;
    }
    return multiply;
}

const calcMul = calculator();
console.log(calcMul());*/
